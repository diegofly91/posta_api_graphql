type Timetable {
    id: Int!
    companyId: Int!
    company: Company!
    dayId: Int!
    day: Day!
    hini: String!
    hend: String!
    isActive: Boolean
    createAdt: String!
    createUpd: String
}

input NewTimetableInput {
  companyId: Int!
  dayId: Int!
  hini: String!
  hend: String!
}

input TimetableInput {
  dayId: Int!
  hini: String!
  hend: String!
  isActive: Boolean
}

input TimetableInputQuery {
  companyId: Int!
  dayId: Int
  isActive: Boolean
}

type Query {
    getTimetableId(id: Int!): Timetable!
   getTimetable(input: TimetableInputQuery!): [Timetable!]!
  # getTimetables(input: TimetableInputQuery, pagination: Pagination): [Employee!]!
}

type Mutation {
  createTimetable(input: NewTimetableInput!): Boolean! 
  updateTimetable(id: Int!, input: TimetableInput!): Boolean!
  deleteTimetable(id: Int!): Boolean!
}
